query SalesRoles {
    salesRoles {
        id
        salesRole
        targetId
        channel
        branch
        status
        userId
        user {
            id
            fullNameTH
            imageUrl
        }
    }
}