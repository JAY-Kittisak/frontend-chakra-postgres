query TargetByRoleId($salesRoleId: Int!, $year: Int!) {
  targetByRoleId(salesRoleId: $salesRoleId, year: $year) {
    id
    year
    commission
    strategy
    countVisit
    countIssue
    valueIssue
    valueQt
    branch
  }
}