mutation CreateSalesTarget($input: SalesTarget_Input!){
    createSalesTarget(input: $input){
        errors{
            field
            message
        }
        salesTargets{
            id
            year
            commission
            strategy
            countVisit
            countIssue
            valueIssue
            valueQt
            branch
            salesRoleId
        }
    }
}