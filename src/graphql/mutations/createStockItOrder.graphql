mutation CreateStockItOrder($input: StockItOrderInput!) {
  createStockItOrder(input: $input) {
    errors {
      field
      message
    }
    stockItOrder {
      ...RegularStockItOrder
    }
  }
}